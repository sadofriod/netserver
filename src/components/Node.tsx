import React from "react";

const Node: React.FC<Partial<Common.Node>> = (props) => {
	const { style, data } = props;
	// const { x, y, height, width } = style;
	// const state = l
	console.log(props);

	return <></>;
};

export default Node;
